import { LoopInnerControlsPipe } from './loop-inner-controls.pipe';

describe('LoopInnerControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new LoopInnerControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
